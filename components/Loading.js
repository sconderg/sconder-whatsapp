import {Circle} from 'better-react-spinkit';

function Loading() {
    return(
        <center>
            <div>
                <Circle color="#3CBC28" size={60} style={{maginTop:50}} />
            </div>
        </center>
    );
}
export default Loading;
